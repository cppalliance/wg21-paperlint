# P4026R0 — Global lookup for begin and end for expansion statements

EWG · wording

Out of scope per metadata classification.

**Possible miscategorization:** The title reads like a design change to expansion statements (a recent feature), not pure standardese cleanup. Your call on whether this should have been evaluated as a proposal.
